<form method="post" action="{{ path('user_duyuru_delete', {'id': duyuru.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ duyuru.id) }}">
    <div class="col-md-2">
    <button class="btn btn-danger">Delete</button>
    </div>
</form>
